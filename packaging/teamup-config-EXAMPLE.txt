# Teamup Dispatch Map — credentials
# ----------------------------------
# 1. Rename this file to exactly:   teamup-config.txt
# 2. Keep it in the SAME folder as the app
# 3. Paste the company values after the = signs (no spaces, no quotes)

# ===== Calendar 1 (e.g. Dispatch / Sales) =====
TEAMUP_API_KEY=
TEAMUP_CALENDAR_ID=
TEAMUP_CALENDAR_NAME=Dispatch

# ===== Calendar 2 (e.g. Production crew) — optional second view =====
# Fill these in to get a "Calendar" dropdown that swaps the whole map between
# the two. Leave them blank for a single-calendar setup (no dropdown shown).
TEAMUP_CALENDAR_ID_2=
TEAMUP_CALENDAR_NAME_2=Production
# Only if the production calendar needs a DIFFERENT Teamup key; otherwise it
# reuses TEAMUP_API_KEY above.
TEAMUP_API_KEY_2=

# (You can add a 3rd, 4th, … the same way: TEAMUP_CALENDAR_ID_3, _NAME_3, etc.)

# ===== Geocoder =====
# "census" is best for rural US addresses; OSM ("nominatim") is the backup.
GEOCODER=census,nominatim
